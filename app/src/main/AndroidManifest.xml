<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sawaedaib.aibrahemsawaed.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/food_app_logo_1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/food_app_logo_1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddFoodActivity" />
        <activity android:name=".LoginTools.LoginActivity" />
        <activity android:name=".LoginTools.SignUpActivity" />
        <activity android:name=".LoginTools.ResetPasswordActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme" />
        <activity android:name=".LoginTools.ToolsActivity" />
        <activity android:name=".AddFoodActivity"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".Test1Activity" />
        <activity android:name=".ImagesActivity"></activity>
    </application>

</manifest>